import type { Metadata } from "next"
export const metadata: Metadata = { title: "Contact - Changeloger", description: "Get in touch with the Changeloger team." }
export default function L({ children }: { children: React.ReactNode }) { return children }
